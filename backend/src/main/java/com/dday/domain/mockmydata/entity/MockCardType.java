package com.dday.domain.mockmydata.entity;

/** Mock 카드의 유형. 서비스 쪽 {@code CardType}과 값이 같아도 따로 둔다 ({@link MockAccountType} 참고). */
public enum MockCardType {

    CREDIT,
    CHECK,
    PREPAID,
    ETC
}
